# TRACR service settings
OCR_API_HOST=0.0.0.0
OCR_API_PORT=8787
OCR_API_BASE_URL=http://127.0.0.1:8787
OCR_WEB_HOST=127.0.0.1
OCR_WEB_PORT=8790

# Data folders (relative to repo root unless absolute)
OCR_INPUTS_DIR=inputs
OCR_OUTPUTS_DIR=outputs
OCR_JOB_CONFIGS_DIR=job_configs
OCR_STATE_DIR=.ocr_state

# Provider keys (used when model mode is API)
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=

# Optional default upstream for raw OpenAI-compatible proxy usage
OCR_DEFAULT_UPSTREAM_BASE_URL=
OCR_DEFAULT_UPSTREAM_API_KEY=

# Local model runtime defaults
OCR_VLLM_BASE_PORT=9000
OCR_VLLM_GPU_MEMORY_UTILIZATION=0.90
OCR_VLLM_MAX_MODEL_LEN=
OCR_VLLM_DATA_PARALLEL_SIZE=1
OCR_VLLM_MAX_CONCURRENT_REQUESTS=8
OCR_LOCAL_MAX_CONCURRENT_MODELS=8
